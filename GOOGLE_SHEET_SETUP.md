# คู่มือการติดตั้งและเชื่อมต่อ Google Sheets สำหรับเว็บไซต์ ALL ACCOUNT

ระบบถูกออกแบบมาเพื่อบันทึกข้อมูลจากแบบฟอร์มบนเว็บไซต์ลงใน **Google Sheets** โดยอัตโนมัติ พร้อม**แยกแท็บ (Sheet)** ตามประเภทของข้อมูล ดังนี้:

| แท็บ (Sheet Name) | ข้อมูลที่บันทึก |
|---|---|
| **1. ติดต่อขอรับบริการ (Contact Form)** | ข้อมูลจากแบบฟอร์มหลักด้านล่างหน้าเว็บ (ชื่อ, เบอร์โทร, LINE ID, บริการที่สนใจ, รายละเอียด) |
| **2. นัดหมายปรึกษา (Modal)** | ข้อมูลจากป็อปอัปนัดหมายปรึกษาฟรี (ชื่อ, เบอร์โทร, บริการที่สนใจ, ข้อความเพิ่มเติม) |
| **3. คำนวณราคาออนไลน์ (Calculator)** | ข้อมูลจากการคำนวณค่าบริการ (รูปแบบธุรกิจ, จำนวนเอกสาร, บริการเสริม, ราคาประเมิน, ข้อมูลผู้ติดต่อ) |

---

## 🛠️ ขั้นตอนการติดตั้ง Google Apps Script (5 นาที)

### ขั้นตอนที่ 1: สร้าง Google Sheets
1. เข้าไปที่ [Google Sheets](https://sheets.new) และสร้างชีตใหม่
2. ตั้งชื่อชีต เช่น **"ALL ACCOUNT - ระบบบันทึกข้อมูลลูกค้า"**

---

### ขั้นตอนที่ 2: เปิด Apps Script
1. ที่เมนูด้านบนของ Google Sheets คลิกที่ **ส่วนขยาย (Extensions)** > **Apps Script**
2. จะเปิดหน้าต่างแก้ไขสคริปต์ขึ้นมา

---

### ขั้นตอนที่ 3: วางโค้ด Code.gs
1. ลบโค้ดเริ่มต้นทั้งหมดในไฟล์ `Code.gs`
2. คัดลอกโค้ดทั้งหมดจากไฟล์ [`Code.gs`](file:///Users/3designs/acc%20all%20acount/Code.gs) ในโปรเจกต์นี้ มาวางลงในหน้าต่าง Apps Script
3. *(ตัวเลือกเสริม)* สามารถใส่อีเมลของคุณในตัวแปร `NOTIFICATION_EMAIL` เพื่อรับการแจ้งเตือนทางอีเมลเมื่อมีลูกค้าส่งข้อมูล
4. กดปุ่ม **บันทึก (ไอคอนแผ่นดิสก์ 💾)** หรือกด `Ctrl + S` / `Cmd + S`

---

### ขั้นตอนที่ 4: เผยแพร่เว็บแอปพลิเคชัน (Deploy Web App)
1. กดปุ่มสีน้ำเงิน **การนำไปใช้งาน (Deploy)** ที่มุมขวาบน > เลือก **การนำไปใช้งานใหม่ (New deployment)**
2. คลิกที่รูปฟันเฟือง ⚙️ ด้านซ้าย > เลือก **เว็บแอปพลิเคชัน (Web app)**
3. ตั้งค่าดังนี้:
   - **คำอธิบาย (Description):** `ALL ACCOUNT Form API`
   - **ดำเนินการในฐานะ (Execute as):** `ฉัน (Me - บัญชีอีเมลของคุณ)`
   - **ผู้ที่มีสิทธิ์เข้าถึง (Who has access):** **`ทุกคน (Anyone)`** *(⚠️ สำคัญมาก ต้องเลือกเป็น Anyone เพื่อให้หน้าเว็บส่งข้อมูลเข้ามาได้)*
4. กดปุ่ม **นำไปใช้งาน (Deploy)**
5. หากมีหน้าต่างขึ้นมาถาม ให้กด **ให้สิทธิ์การเข้าถึง (Authorize access)** > เลือกบัญชี Google ของคุณ > กด **Advanced** > กด **Go to ... (unsafe)** > กด **Allow**
6. เมื่อเสร็จสิ้น จะมีหน้าต่างแสดง **URL ของเว็บแอปพลิเคชัน (Web App URL)** เช่น:
   ```
   https://script.google.com/macros/s/AKfycbx.../exec
   ```
7. กด **คัดลอก (Copy)** URL นั้นไว้

---

### ขั้นตอนที่ 5: นำ URL ไปใส่ในเว็บไซต์
1. เปิดไฟล์ [`js/app.js`](file:///Users/3designs/acc%20all%20acount/js/app.js) ในโปรเจกต์
2. นำ URL ที่คัดลอกมา ไปวางในตัวแปร `GOOGLE_SCRIPT_URL` ที่บรรทัดด้านบนสุด:
   ```javascript
   const GOOGLE_SCRIPT_URL = "https://script.google.com/macros/s/AKfycbx.../exec";
   ```
3. บันทึกไฟล์ `js/app.js` เป็นอันเสร็จสมบูรณ์!

---

## 🧪 การทดสอบระบบ
1. เปิดไฟล์ `index.html` บนเบราว์เซอร์
2. ทดลองกรอกแบบฟอร์มหน้าเว็บ หรือกดปุ่ม "ปรึกษาฟรี" แล้วกรอกข้อมูลส่ง
3. กลับไปดูที่ Google Sheets จะพบว่าระบบสร้างแท็บ Sheet และบันทึกแถวข้อมูลให้อัตโนมัติทันที
